local draw = require 'draw_utils'

return function ()
  local s = {}
  local W, H = W, H

  s.press = function (x, y)
  end

  s.hover = function (x, y)
  end

  s.move = function (x, y)
  end

  s.release = function (x, y)
  end

  s.update = function ()
  end

  s.draw = function ()
  end

  s.destroy = function ()
  end

  return s
end
